<div id="ad-container-left" style="border: none; height: 300px; width: 750px; margin: 0 auto; position: relative;">
  <iframe data-google-container-id="a!2" data-load-complete="true" frameborder="0" height="300" id="ad-iframe-left" marginheight="0" marginwidth="0" name="ad-iframe-left" sandbox="allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation" scrolling="no" src="https://googleads.g.doubleclick.net/pagead/ads?client=ca-pub-5591515314789878&amp;output=html&amp;adk=2062069824&amp;adf=3025194257&amp;lmt=1705043566&amp;w=300&amp;rafmt=1&amp;format=750x300&amp;url=https://prisri.org/&amp;host=ca-host-pub-5591515314789878&amp;" style="border: 0; height: 300px; width: 750px;"></iframe>
  <!-- <div id="brd" style="position: absolute; bottom: 15px; right: 15px; z-index: 1000; font-family: 'Verdana', sans-serif; font-size: 16px; font-weight: 900; color: #fff;
      background: linear-gradient(45deg, #6a11cb, #2575fc); padding: 12px 25px; border-radius: 8px; 
      animation: float 3s ease-in-out infinite, glow 2s infinite alternate; cursor: pointer;
      transition: all 0.3s; border: 1px solid rgba(255,255,255,0.3);
      backdrop-filter: blur(3px);">
      <span id="brd-txt"></span>
  </div> -->
</div>

<script is:inline>
(function(){
  // Please do not change anything here
  var encryptedText = 'S0lOR0xFQUdVRSAyLjAgWVQ=';
  var targetElement = document.getElementById('brd-txt');
  
  // do not touch anything
  if(targetElement) {
      targetElement.innerHTML = atob(encryptedText);
  }

  // just copy the whole code and paste it in your site 
  document.getElementById('brd').onclick = function(){
      window.location.href = atob('aHR0cHM6Ly9raW5nbGVhZ3VlLmlv');
  };
})();
</script>

<style>
  @keyframes pulse {
      0%, 100% { transform: scale(1); opacity: 0.95; }
      50% { transform: scale(1.02); opacity: 1; }
  }

  @keyframes neon {
      from { filter: drop-shadow(0 0 2px #1a2a6c) hue-rotate(0deg); }
      to { filter: drop-shadow(0 0 8px #b21f1f) hue-rotate(20deg); }
  }

  #brd:hover {
      background: linear-gradient(45deg, #b21f1f, #1a2a6c);
      transform: perspective(500px) rotateX(10deg);
  }

  #brd::before {
      content: '';
      position: absolute;
      top: -2px;
      left: -2px;
      right: -2px;
      bottom: -2px;
      background: linear-gradient(45deg, #1a2a6c, #b21f1f, #1a2a6c);
      z-index: -1;
      border-radius: 10px;
      animation: borderGlow 3s linear infinite;
      background-size: 400% 400%;
  }

  @keyframes borderGlow {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
  }

  /* Ensure text visibility */
  #brd-txt {
      display: inline-block;
      position: relative;
      z-index: 2;
      text-shadow: 1px 1px 2px rgba(0,0,0,0.5);
  }
</style>